---
title: Toka Programming Language
description: A no-GC systems programming language that keeps performance and static safety as baselines while making real systems semantics explicit and everyday code easier to read, write, and maintain.
template: splash
hero:
  tagline: "A no-GC systems programming language that keeps performance and static safety as baselines while making real systems semantics explicit and everyday code easier to read, write, and maintain."
  banner:
    content: "v1.0.0-rc.4 is in qualification. v0.9.9-01 remains the latest stable SDK."
  image:
    file: ../../assets/logo.svg
  actions:
    - text: Try Toka Online
      link: /playground/
      icon: rocket
      variant: primary
    - text: Developer Guide
      link: /introduction/
      icon: right-arrow
      variant: secondary
    - text: Replicate a Package
      link: https://github.com/tokalang/toka/blob/main/AGENTS-USER.md
      icon: external
      variant: minimal
    - text: View on GitHub
      link: https://github.com/tokalang/toka
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Design Goal

Toka starts from two baselines: predictable zero-cost performance and strong static safety. With those in place, the language asks how direct, compact, and maintainable systems code can become without hiding the semantics that make systems programming hard.

Toka does not pursue minimal syntax for its own sake. It pursues the minimum surface needed to expose real systems semantics: ownership transfer, mutation, rebinding, nullability, error propagation, async suspension, FFI boundaries, and representation-level access.

<CardGrid stagger>
	<Card title="Performance As A Baseline" icon="rocket">
		Toka has no garbage collector and is compiled ahead of time through LLVM. Resource and representation costs are intended to stay visible and predictable rather than being hidden behind a default runtime layer.
	</Card>
	<Card title="Static Safety As A Baseline" icon="shield">
		Resource flow, borrowing, nullability, rebinding, and cleanup are checked through explicit contracts such as PAL and `cede`. When a relationship cannot be proven locally, Toka should be conservative rather than push a full lifetime calculus into ordinary code.
	</Card>
	<Card title="Payload And Handle Separation" icon="puzzle">
		Plain names operate on object payloads. Hats such as `&`, `*`, `^`, and `~` expose handle identity only when code genuinely needs to borrow, own, share, forward, or rebind that layer.
	</Card>
	<Card title="Complexity With A Job" icon="star">
		Every piece of surface complexity should earn its place by revealing a systems boundary that would otherwise become implicit, distant, or harder to audit.
	</Card>
	<Card title="Integrated Project Tooling" icon="setting">
		The `toka` command manages project creation, single-file runs, package resolution, build orchestration, tests, formatting, and documentation around `package.tk` and `build.tk`.
	</Card>
</CardGrid>

## Where To Start

- Read [What is Toka?](/introduction/) for the core language model.
- Install the current release from [Installation](/installation/).
- Try small examples in the [online playground](/playground/).
- Follow the community-driven [Toka Book](https://lumicore-dev.github.io/toka-book/) for a longer tutorial path.
