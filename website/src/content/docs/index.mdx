---
title: Toka Programming Language
description: A no-GC systems programming language that keeps performance and static safety as baselines while making code simpler to read, write, and maintain.
template: splash
hero:
  tagline: "A no-GC systems programming language that keeps performance and static safety as baselines while making code simpler to read, write, and maintain."
  banner:
    content: "v0.9.8-06 is available with build-system hardening, cross-platform packaging fixes, and refreshed documentation."
  image:
    file: ../../assets/logo.svg
  actions:
    - text: Try Toka Online
      link: /playground/
      icon: rocket
      variant: primary
    - text: Developer Guide
      link: /introduction/
      icon: right-arrow
      variant: secondary
    - text: View on GitHub
      link: https://github.com/tokalang/toka
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Design Goal

Toka starts from two baselines: predictable zero-cost performance and strong static safety. With those in place, the language asks how direct, compact, and maintainable systems code can become.

The aim is not to make low-level behavior disappear. It is to make the important parts visible where they matter: ownership transfer, mutation, rebinding, nullability, error propagation, async suspension, and representation-level access.

<CardGrid stagger>
	<Card title="Performance As A Baseline" icon="rocket">
		Toka has no garbage collector and is compiled ahead of time through LLVM. Resource and representation costs are intended to stay visible and predictable rather than being hidden behind a default runtime layer.
	</Card>
	<Card title="Static Safety As A Baseline" icon="shield">
		Resource flow, borrowing, nullability, rebinding, and cleanup are checked through explicit contracts such as PAL and `cede`, without requiring user-written lifetime parameters in ordinary code.
	</Card>
	<Card title="Payload And Handle Separation" icon="puzzle">
		Plain names operate on object payloads. Hats such as `&`, `*`, `^`, and `~` expose handle identity only when code genuinely needs to borrow, own, share, forward, or rebind that layer.
	</Card>
	<Card title="Integrated Project Tooling" icon="setting">
		The `toka` command manages project creation, single-file runs, package resolution, build orchestration, tests, formatting, and documentation around `package.tk` and `build.tk`.
	</Card>
</CardGrid>

## Where To Start

- Read [What is Toka?](/introduction/) for the core language model.
- Install the current release from [Installation](/installation/).
- Try small examples in the [online playground](/playground/).
- Follow the community-driven [Toka Book](https://lumicore-dev.github.io/toka-book/) for a longer tutorial path.
