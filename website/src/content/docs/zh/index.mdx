---
title: Toka 编程语言
description: 一门以零成本性能和静态安全为底线，同时让代码更易读、易写、易维护的无 GC 系统编程语言。
template: splash
hero:
  tagline: "一门以零成本性能和静态安全为底线，同时让代码更易读、易写、易维护的无 GC 系统编程语言。"
  banner:
    content: "v0.9.8-06 已发布，包含构建系统加固、跨平台打包修复，以及文档刷新。"
  image:
    file: ../../../assets/logo.svg
  actions:
    - text: 在线运行 Toka
      link: /playground/
      icon: rocket
      variant: primary
    - text: 开发指南
      link: /zh/introduction/
      icon: right-arrow
      variant: secondary
    - text: GitHub 仓库
      link: https://github.com/tokalang/toka
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## 设计目标

Toka 从两个底线出发：可预测的零成本性能，以及强静态安全。在这两个前提下，Toka 追问的是：系统代码还能不能更直接、更紧凑、更可维护。

Toka 并不试图把底层行为藏起来。它希望真正重要的事情在源码中可见：所有权转移、可变性、重绑定、可空性、错误传播、异步挂起，以及表示层访问。

<CardGrid stagger>
	<Card title="性能是底线" icon="rocket">
		Toka 没有垃圾回收器，并通过 LLVM 进行 AOT 原生编译。资源成本和表示成本应当保持可见、可预测，而不是被默认运行时层遮住。
	</Card>
	<Card title="静态安全是底线" icon="shield">
		资源流、借用、可空性、重绑定和清理通过 PAL、`cede` 等显式契约接受检查，同时避免把用户手写生命周期参数变成日常负担。
	</Card>
	<Card title="Payload 与 Handle 分离" icon="puzzle">
		裸名操作对象 payload。只有代码确实需要借用、拥有、共享、转发或重绑定 handle 层时，才使用 `&`、`*`、`^`、`~` 等帽子。
	</Card>
	<Card title="内建项目工具链" icon="setting">
		`toka` 命令围绕 `package.tk` 和 `build.tk` 管理项目创建、单文件运行、包解析、构建编排、测试、格式化和文档生成。
	</Card>
</CardGrid>

## 从哪里开始

- 阅读 [Toka 是什么？](/zh/introduction/) 了解核心语言模型。
- 查看 [安装指南](/zh/installation/) 安装当前 release。
- 在 [在线 Playground](/playground/) 尝试小例子。
- 通过社区维护的 [Toka Book](https://lumicore-dev.github.io/toka-book/) 系统学习。
