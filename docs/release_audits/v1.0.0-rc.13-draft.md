# RC13 draft asset delivery — 2026-09-25

Status: **draft created and asset replayed; not promoted or published**.

- Candidate and annotated tag `v1.0.0-rc.13` peel to
  `6fb3337fa4f9956392326a7783c9dcdecabd7e5b`.
- [Four-target qualification run 36093979008](https://github.com/tokalang/toka/actions/runs/36093979008)
  completed successfully at that exact SHA. Its downloaded
  `toka.release-qualification-summary` has `result=pass`, `errors=[]`, and
  passing reports for Linux x64/ARM64 and macOS x64/ARM64.
- A second tag-ref qualification run, 36117012266, was cancelled at the
  user's request because it repeated four builds of the same source SHA.
  It supplies no qualification evidence and did not create this draft.
- [The RC13 draft](https://github.com/tokalang/toka/releases/tag/untagged-143f1a34e8de9cf5a4f9)
  was created from the four preserved `candidate-archive-*` artifacts of the
  successful run. The draft remains `isDraft=true`, `isPrerelease=true` and
  contains exactly those four archives plus `SHA256SUMS`. Its corrected notes
  are in `docs/release_notes_v1.0.0-rc.13.md`; that later documentation commit
  does not change the tagged candidate.

## Actual draft assets

The four tarballs and `SHA256SUMS` were downloaded **back from the draft** into
`validation/rc13-draft-reuse-20260925/draft-actual/`. The repository's
`verify_release_assets.py --require-checksums` passed there. The downloaded
manifest byte-for-byte matches the one generated from the successful-run
artifacts. SHA-256 values of the actual draft tarballs:

| Asset | SHA-256 |
| --- | --- |
| `toka-v1.0.0-rc.13-linux-arm64.tar.gz` | `a9cb51c2e9d5a6ef65550b90d74c43a0e168efd7fc7a6a1289ad5527d7711153` |
| `toka-v1.0.0-rc.13-linux-x64.tar.gz` | `c59687caadbcb126f7594d0d069f794b717b67981405e1181537cf45dd7e208d` |
| `toka-v1.0.0-rc.13-macos-arm64.tar.gz` | `0185cc78f1e38d4f182b9e73f90e329753735e935ea6ddface67a6f854794e9c` |
| `toka-v1.0.0-rc.13-macos-x64.tar.gz` | `cefc8832f4391bf04b8276109ad966c42a84b08a16fe4248f80287d7249c63e7` |

The actual draft `SHA256SUMS` file itself hashes to
`0f11d3057e770577066f44ab139bcf12e006e2d893b41ea0685b11dda8189baf`.

GitHub's uploaded-asset digests match these independently computed tarball
digests. They are **tarball** digests, not Actions artifact ZIP digests.

## Clean extraction and consumers

- The actual draft macOS ARM64 archive passed `test_release_package.py`:
  **18/18**, including compiler/manager versions, compile+run, a source-hidden
  resource graph, POSIX file lifecycle, project creation/run, preview and
  local-package lock/offline/remove.
- After extraction and relocation to a directory containing a space, with
  `TOKA_LIB`, `TOKAC` and `TOKA_PATH` unset, PATH-invoked `toka --version`,
  `doctor`, `new`, `run`, `check src/main.tk`, another locked `run` and
  `doctor` succeeded. A preliminary `toka check` without a required source
  argument was an invocation error, not an SDK failure; the explicit-source
  replay passed.
- [Hosted macOS x64 replay run 36119571046](https://github.com/tokalang/toka/actions/runs/36119571046)
  passed on `macos-15-intel`. Its uploaded
  `toka.qualified-artifact-replay` receipt records candidate SHA
  `6fb3337fa4f9956392326a7783c9dcdecabd7e5b`, qualification run
  `36093979008`, `asset_source=candidate_run`, archive SHA-256
  `cefc8832f4391bf04b8276109ad966c42a84b08a16fe4248f80287d7249c63e7`
  and `result=pass`. The workflow verified tag/qualification identity,
  SHA-256, clean extraction, doctor, new/run, offline local dependency,
  preview and registry offline replay.

The Intel runner's read-only token could not directly see the draft (`release
not found` in diagnostic run 36119370265). The successful hosted run therefore
downloaded the successful qualification run's candidate archive, not the draft
endpoint. The actual draft archive was separately downloaded and verified to
have the **same SHA-256**, so the hosted executable bytes were identical.
This distinction is retained rather than describing the run as a direct draft
download or raising the runner's token to release-write permission.

No additional four-platform build, tag move, promotion, public release, PR, E
implementation, or regex/`trg` qualification is claimed by this record.
