# RC13 promotion and public-download receipt — 2026-09-25

Status: **public prerelease**, not marked Latest and not a Toka 1.0 stability
release. The annotated `v1.0.0-rc.13` tag still peels to the four-platform
qualified candidate `6fb3337fa4f9956392326a7783c9dcdecabd7e5b`.

- Before promotion, only the draft body was updated: its outdated draft-status
  sentence was removed and two RFC links were pinned to the candidate SHA.
  The migration boundary, known limits and non-stability statement remain.
  The source-note edit is documentation-only commit `b8c60f96`, after the tag.
- [Protected promotion run 36125232137](https://github.com/tokalang/toka/actions/runs/36125232137)
  succeeded with the previously reviewed hosted replay
  [run 36119571046](https://github.com/tokalang/toka/actions/runs/36119571046)
  as its receipt input. It verified draft/prerelease state, exact five asset
  names, re-downloaded all assets and checked `SHA256SUMS` before publishing.
- The [public RC13 page](https://github.com/tokalang/toka/releases/tag/v1.0.0-rc.13)
  returned HTTP 200 without GitHub credentials. GitHub reports
  `isDraft=false`, `isPrerelease=true`, five uploaded assets. The repository's
  Latest release remains `v0.9.9-01`; RC13 was not made Latest.
- Public unauthenticated requests returned HTTP 200 for each of the four
  platform archive URLs. The public `SHA256SUMS` download byte-for-byte
  matched the reviewed draft manifest. No archive was replaced: the five
  post-promotion GitHub asset digests still match the exact values in the
  [draft asset record](v1.0.0-rc.13-draft.md).
- The macOS ARM64 tarball was independently re-downloaded through its **public
  release URL**. Its SHA-256 was
  `0185cc78f1e38d4f182b9e73f90e329753735e935ea6ddface67a6f854794e9c`.
  A clean extraction under `validation/rc13-draft-reuse-20260925/public-smoke/`
  ran packaged `tokac --version`, `toka --version`, `toka doctor`, `toka new`
  and `toka run` successfully, producing `Hello, Toka!`. The SDK variables
  `TOKA_LIB`, `TOKAC` and `TOKA_PATH` were unset. This is a clean-extraction
  installation smoke, **not** a claim that the user-home `install.sh` was run.

No tag move, archive replacement, four-platform rebuild, regex/`trg`
qualification or new language implementation is attributed to promotion.
