# v1.0.0-rc.5 qualification audit

**Status:** Awaiting evidence. No RC5 candidate SHA has been selected, and
`v1.0.0-rc.5` is not tagged, drafted, or published. Every unknown field below
is intentionally marked pending; this document authorizes no release action.

## Compatibility boundary

- Expected `.tki` metadata format: `2`
- Expected compiler-interface compatibility key: `0.9.9-10`
- RC4 cache reuse: prohibited

RC4 `.tki`, object, and semantic-cache artifacts are incompatible release
inputs for RC5. Qualification, packaging, and installed-archive replay must
start from clean cache state.

## Candidate identity

| Evidence | Record |
| --- | --- |
| Immutable candidate SHA | Pending |
| Candidate source is clean | Pending |
| Release label is `v1.0.0-rc.5` | Pending |
| `.tki` metadata format is `2` | Pending |
| Interface compatibility key is `0.9.9-10` | Pending |
| Qualification workflow run | Pending |

## Four-target qualification

The required target set is fixed. A row is complete only when its
`toka.release-gate` v2 report binds the selected SHA, records
`source_dirty=false` and `version_label=v1.0.0-rc.5`, and passes all thirteen
stages. Its TaskHandle Lifecycle v2 record must bind the same SHA and common
contract digest.

| Target | Release-gate report | TaskHandle v2 record |
| --- | --- | --- |
| Linux x64 | Pending | Pending |
| Linux arm64 | Pending | Pending |
| macOS x64 | Pending | Pending |
| macOS arm64 | Pending | Pending |

Qualification summary: **Pending.** No stage or target is recorded as passing
until the hosted evidence is linked and verified together.

RC4-cache exclusion evidence: **Pending.** The record must show clean build,
semantic replay, and packaging inputs for all four targets.

## Draft assets and clean replay

| Evidence | Record |
| --- | --- |
| Annotated tag authorization | Pending |
| Tag workflow run | Pending |
| Draft release URL | Pending |
| Exact four-archive asset list | Pending |
| `SHA256SUMS` asset | Pending |

The tag workflow may create only a draft. Record each archive digest and replay
receipt from a clean extraction; do not reuse a local build tree or RC4 cache.

| Target archive | SHA-256 | Clean first-hour replay receipt |
| --- | --- | --- |
| Linux x64 | Pending | Pending |
| Linux arm64 | Pending | Pending |
| macOS x64 | Pending | Pending |
| macOS arm64 | Pending | Pending |

The replay must cover `toka doctor`, `toka new`, registry resolution, `toka
run`, offline lock replay, and `toka preview` for each final archive.

## Protected promotion

| Evidence | Record |
| --- | --- |
| Complete replay receipt supplied to promotion workflow | Pending |
| Protected maintainer approval | Pending |
| Draft asset set reverified without mutation | Pending |
| Public pre-release promotion | Pending |

Any pending, missing, dirty, reused-cache, or mismatched record keeps RC5
fail-closed and blocks public promotion.
