import std/task::{block_on}

fn worker(value: i32) -> async i32 {
    return value * 2
}

fn main() -> i32 {
    auto task = worker(7).start
    if block_on<i32>(task) != 14 { return 1 }
    return 0
}
