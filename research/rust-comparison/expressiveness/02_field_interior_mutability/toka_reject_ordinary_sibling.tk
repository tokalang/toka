// Declaring reads# does not grant payload-write authority to ordinary.

shape Counter(
    reads#: i32,
    ordinary: i32
)

fn main() -> i32 {
    auto counter = Counter(reads = 0, ordinary = 9)
    auto &shared = &counter
    shared.ordinary = 10
    return 0
}
