trait @Fetcher {
    pub fn fetch(self) -> async i32
}

shape Immediate()

impl Immediate@Fetcher {
    pub fn fetch(self) -> async i32 {
        return 17
    }
}

fn consume<T: @Fetcher>(worker: T) -> async i32 {
    return worker.fetch().await
}

fn main() -> i32 {
    auto worker = Immediate()
    auto task = consume<Immediate>(worker).start
    return 0
}

// EXPECT: error[E04583]
