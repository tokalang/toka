// A payload-writable binding does not become handle-rebindable at the use site.

shape Cell(value: i32)

fn replace_handle(^#p: Cell) {
    ^#p = new Cell(value = 17)
}

fn main() -> i32 {
    auto ^payload_only# = new Cell(value = 1)
    replace_handle(^#payload_only)
    return 0
}
