// A handle-rebindable binding does not become payload-writable at the use site.

shape Cell(value: i32)

fn overwrite_payload(^p#: Cell) {
    p.value = 13
}

fn main() -> i32 {
    auto ^#handle_only = new Cell(value = 1)
    overwrite_payload(^#handle_only)
    return 0
}
