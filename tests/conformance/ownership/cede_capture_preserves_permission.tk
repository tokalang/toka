shape Cell (
    value#: i32
)

fn main() -> i32 {
    auto cell# = Cell(value = 1)
    auto invoke# = { [cede cell#] =>
        cell.value = 2
        return cell.value
    }:fn#() -> i32
    assert(invoke#() == 2,
           "cede capture retains declared payload capability")
    return 0
}
