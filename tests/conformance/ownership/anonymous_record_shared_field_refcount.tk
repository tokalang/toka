shape Resource (
    value: i32
)

fn main() -> i32 {
    auto ~root# = new Resource(value = 9)
    auto graph = (root = ~root)
    auto copied_graph = (root = graph.~root)
    if copied_graph.root.value != 9 {
        return 1
    }
    return 0
}
