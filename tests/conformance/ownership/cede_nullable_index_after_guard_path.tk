shape Token(id: i32)

fn main() -> i32 {
    auto values = [Token(id = 9)]:[Token?; 1]
    guard values[0] {
        auto target = cede values[0]:Token
        return target.id - 9
    } else {
        return 1
    }
}
