shape Token(id: i32)

fn main() -> i32 {
    auto values = [Token(id = 11)]:[Token?; 1]
    if values[0] is null {
        return 1
    } else {
        auto target = cede values[0]:Token
        return target.id - 11
    }
}
