shape Token(id: i32)
shape Pair(item: Token?)

fn main() -> i32 {
    auto pair = Pair(item = Token(id = 7))
    guard pair.item {
        auto target: Token = cede pair.item
        return target.id - 7
    } else {
        return 1
    }
}
