shape Mutator()

impl Mutator@Callable {
    fn call(self, value#: i32) {
        value = 2
    }
}

fn main() -> i32 {
    auto callback = Mutator()
    auto value# = 1
    callback(value)
    if value != 2 { return 1 }
    return 0
}
