shape Cell(value: i32)

impl Cell {
    fn overwrite(cede self#) -> cede Cell {
        self.value = 2
        return cede self
    }
}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto result = source#.overwrite()
    return result.value - 2
}
