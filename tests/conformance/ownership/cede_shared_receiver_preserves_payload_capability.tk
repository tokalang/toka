shape Cell(value: i32)

impl Cell {
    fn overwrite(self#) -> i32 {
        self.value = 2
        return self.value
    }
}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    return source#.overwrite() - 2
}
