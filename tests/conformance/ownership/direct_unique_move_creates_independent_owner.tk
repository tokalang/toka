shape Cell(value: i32)

fn main() -> i32 {
    auto ^source = new Cell(value = 1)
    auto ^view# = ^source
    view.value = 2
    assert(view.value == 2,
           "a direct hatted unique move creates an independent writable owner")
    return 0
}
