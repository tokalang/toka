shape Token(id: i32)

fn main() -> i32 {
    auto arr = [Token(id = 7)]:[Token?; 1]
    auto target = cede arr[0]
    guard target {
        return target.id - 7
    } else {
        return 1
    }
}
