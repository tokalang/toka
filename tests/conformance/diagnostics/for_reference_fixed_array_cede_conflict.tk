shape Token(id: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values = [Token(id = 1), Token(id = 2)]:[Token; 2]
    for auto &value in values {
        auto taken = cede values[1]
        return taken.id
    }
    return 0
}
// EXPECT_ERROR: E0440
