shape Token(id: i32)

impl Token {
    fn consume(cede token: Token) -> i32 {
        auto owned = cede token
        return owned.id
    }
}

fn main() -> i32 {
    auto ^source# = new Token(id = 1):^Token?
    return Token::consume(cede source)
}
