import core/option::{Option}

fn main() -> i32 {
    auto value = 1
    auto opt = Option<&i32>::Some(&value)
    guard auto Option<&i32>::Some(&view#) = opt else {
        return 0
    }
    view = 2
    return 0
}
