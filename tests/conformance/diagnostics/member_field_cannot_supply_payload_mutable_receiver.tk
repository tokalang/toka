shape Cell(value: i32)

impl Cell {
    fn set(self#, value: i32) {
        self.value = value
    }
}

shape Holder(
    ~cell: Cell
)

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto holder# = Holder(~cell = ~source)
    holder.~cell.set(12)
    return 0
}
