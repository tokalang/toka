import core/traits::{@Encap}

shape Token(id: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values: [Token; 1] = [Token(id = 1)]
    match values[0] {
        auto &borrow => {
            auto taken = cede values[0]
            return taken.id
        }
    }
}
// EXPECT_ERROR: E0440
