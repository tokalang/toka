import core/traits::{@Encap}

shape Token(id: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values = [Token(id = 7)]:[Token; 1]
    auto index = 0
    auto taken = cede values[index]
    return taken.id
}
