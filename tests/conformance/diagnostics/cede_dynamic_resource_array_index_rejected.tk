import core/traits::{@encap}

shape Token(id: i32)

impl Token@encap {
    fn drop(self#) {}
    pub fn clone(self) = delete
}

fn main() -> i32 {
    auto values: [Token; 1] = [Token(id = 7)]
    auto index = 0
    auto taken = cede values[index]
    return taken.id
}
