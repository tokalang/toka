import core/traits::{@Encap}

shape Token(id: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values = [Token(id = 1)]:[Token; 1]
    guard auto &borrow = values[0] else { return 1 }
    auto taken = cede values[0]
    return taken.id
}
// EXPECT_ERROR: E0440
