shape Cell(value: i32)

shape Holder(
    ~cell#: Cell
)

fn main() -> i32 {
    auto holder = Holder(~cell = new Cell(value = 2))
    auto ~source = new Cell(value = 1)
    auto switch# = true
    if switch {
        holder.~cell = cede ~source
    }
    holder.cell.value = 3
    return 0
}
