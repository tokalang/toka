import core/string::{string}

shape Resource(name: string)
impl Resource@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto first = Resource(name = string::from("first"))
    auto second# = Resource(name = string::from("second"))
    second = first
    return 0
}
