import core/traits::{@Encap}

shape Token(id: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values = [Token(id = 1)]:[Token; 1]
    if true {
        auto taken = cede values[0]
    }
    return values[0].id
}
