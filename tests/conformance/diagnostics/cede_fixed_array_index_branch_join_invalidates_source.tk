import core/traits::{@encap}

shape Token(id: i32)

impl Token@encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto values: [Token; 1] = [Token(id = 1)]
    if true {
        auto taken = cede values[0]
    }
    return values[0].id
}
