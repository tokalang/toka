shape Token(id: i32)

fn main() -> i32 {
    auto values: [Token?; 1] = [Token(id = 7)]
    auto index = 0
    guard values[index] {
        auto target: Token = cede values[index]
        return target.id
    } else {
        return 0
    }
}
