import std/thread::thread_spawn

fn main() -> i32 {
    auto value = 7
    auto f = { =>
        return cede value
    }:fn() -> i32
    auto g = { =>
        return 0
    }:fn() -> i32
    g = cede f
    auto handle# = thread_spawn<i32>(cede g)
    return 0
}
