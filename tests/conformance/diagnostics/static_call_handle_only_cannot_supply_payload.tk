shape Cell(value#: i32)

impl Cell {
    fn overwrite(^p#: Cell) {
        p.value = 9
    }
}

fn main() -> i32 {
    auto ^#handle_only = new Cell(value = 1)
    Cell::overwrite(^#handle_only)
    return 0
}
