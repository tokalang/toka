import core/traits::{@encap}

shape Owner (
    value: string
)

impl Owner@encap {
    fn drop(self#) {}
}

shape Holder (
    value: string
)

impl Holder@encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto owner = Owner(value = string::from("must transfer"))
    auto holder = Holder(value = owner.value)
    return 0
}
