shape Cell(
    value: i32
)

shape Holder(
    ~cell: Cell
)

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto holder# = Holder(~cell = ~source)
    holder.cell.value = 2
    return 0
}
