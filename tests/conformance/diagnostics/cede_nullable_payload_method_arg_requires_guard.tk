shape Token(id: i32)
shape Sink()

impl Sink {
    pub fn consume(self, cede token: Token) -> i32 {
        auto owned = cede token
        return owned.id
    }
}

fn main() -> i32 {
    auto sink = Sink()
    auto ^source#: Token? = new Token(id = 1)
    return sink.consume(cede source)
}
