import std/vec::{Vec}

shape SocketResource (
    id: i32
)

impl SocketResource@Encap {
    pub id
    fn drop(self#) {}
}

fn main() -> i32 {
    auto v# = Vec<SocketResource>::new()
    v#.push(cede SocketResource(id = 5))
    auto s = v.at(0:usize)
    return 0
}
