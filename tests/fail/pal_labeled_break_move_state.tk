shape Data(id: i32)

fn main() -> i32 {
    auto ^p# = new Data(id = 1)

    auto outer = loop {
        loop {
            auto ^taken = cede ^p // EXPECT: error[E04501]
            if taken.id == 1 {
                break to outer
            }
        }
    }:()

    return 0
}
