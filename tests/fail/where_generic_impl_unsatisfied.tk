trait @Marked {}

shape Plain(value: i32)

shape Box<T>(item: T)

impl<T> Box<T>
where:
    T impl @Marked
{
    pub fn marker(self) -> i32 {
        return 7
    }
}

pub fn main() -> i32 {
    auto box = Box<Plain>(item = Plain(value = 1))
    return box.marker() // EXPECT: error[E0419]
}
