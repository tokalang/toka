// CHECK: error[E0410]
import core/traits::{@Encap}

shape Pair (
    x: i32,
    y: i32
)

impl Pair@Encap {
    pub x
    pub y
    fn drop(self#) {}
}

fn main() -> i32 {
    auto pair# = uninit:Pair
    auto &borrow# = &pair
    borrow.x = 1
    return pair.y
}
