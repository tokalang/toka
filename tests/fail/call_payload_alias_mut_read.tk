shape Data(val: i32)

fn mut_and_read(target#: Data, view: Data) -> i32 {
    target.val = 10
    return view.val
}

fn main() -> i32 {
    auto x# = Data(val = 1)
    return mut_and_read(x, x) // EXPECT: error[E0475]
}
