import std/vec::{Vec}

shape LockResource (
    held: bool
)

impl LockResource@Encap {
    pub held
    fn drop(self#) {}
}

fn main() -> i32 {
    auto v# = Vec<LockResource>::new()
    v#.push(cede LockResource(held = true))
    auto opt = v.get_opt(0:usize)
    return 0
}
