import std/iter::{map}
import std/vec::{Vec}

fn main() -> i32 {
    auto values# = Vec<i32>::new()
    values#.push(1)
    auto source = values.into_iter()
    auto once = { value => return value }:cede fn(i32) -> i32
    auto mapped = map(cede source, cede once)
    return 0
}
