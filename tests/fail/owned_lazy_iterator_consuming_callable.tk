import std/iter::{map}
import std/vec::{Vec}

fn main() -> i32 {
    auto values# = Vec<i32>::new()
    values#.push(1)
    auto source = values.into_iter()
    auto once: cede fn(i32) -> i32 = { value => return value }
    auto mapped = map(cede source, cede once)
    return 0
}
