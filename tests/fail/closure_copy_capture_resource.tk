// EXPECT: error[E04581]

import core/traits::{@Encap}

shape Env(value: i32)

impl Env@Encap {
    fn drop(self#) {}
}

fn main() -> i32 {
    auto env = Env(value = 5)
    auto f: fn() -> i32 = { [copy env] => env.value }
    return f()
}
