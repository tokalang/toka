shape Counter(value#: i32)

fn mutate(c#: Counter) {
    c.value = c.value + 1
}

fn main() -> i32 {
    auto c# = Counter(value = 0)
    mutate(c# as Counter)
    return 0
}

// EXPECT: error[E0461]
