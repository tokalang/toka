// CHECK: error[E04617]
import core/traits::{@Encap}

shape Pair (
    x: i32,
    y: i32
)

impl Pair@Encap {
    pub x
    pub y
    fn drop(self#) {}
}

fn main() -> i32 {
    auto pair# = uninit:Pair
    pair.x = 1
    return 0
}
