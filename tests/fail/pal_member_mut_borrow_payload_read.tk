// EXPECT: error[E0441]

shape CounterInfo(
    accesses#: i32,
    name: str
)

fn main() -> i32 {
    auto info# = CounterInfo(accesses = 0, name = "metrics")
    auto &field_view# = info.&accesses

    auto value = info.accesses
    return field_view + value
}
