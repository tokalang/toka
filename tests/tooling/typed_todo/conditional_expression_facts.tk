fn identity(value: i32) -> i32 {
    return value
}

fn main() -> i32 {
    auto answer = todo:i32
    auto arithmetic = answer + 1:i32
    auto through_call = identity(arithmetic):i32
    return through_call
}
