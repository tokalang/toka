fn select(flag: bool) -> i32 {
    auto answer: i32 = todo
    auto slot#: i32 = 0
    if flag {
        slot = answer
    } else {
        slot = 0
    }
    auto observed: i32 = slot
    return observed
}

fn main() -> i32 {
    return select(true)
}
