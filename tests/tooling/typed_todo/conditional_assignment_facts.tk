fn select(flag: bool) -> i32 {
    auto answer = todo:i32
    auto slot# = 0:i32
    if flag {
        slot = answer
    } else {
        slot = 0
    }
    auto observed = slot:i32
    return observed
}

fn main() -> i32 {
    return select(true)
}
