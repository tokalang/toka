fn select() -> i32 {
    auto answer = todo:i32
    auto slot# = 0:i32
    loop false {
        slot = answer
    }
    auto observed = slot:i32
    return observed
}

fn main() -> i32 {
    return select()
}
