fn select(cond: bool) -> i32 {
    auto answer = todo:i32
    auto live = if cond {
        pass answer
    } else {
        pass 0
    }:i32
    auto dead = if true {
        pass 0
    } else {
        pass answer
    }:i32
    return live + dead
}

fn main() -> i32 {
    return select(true)
}
