fn select(tag: i32) -> i32 {
    auto answer = todo:i32
    auto chosen = match tag {
        0 => { pass answer }
        _ => { pass 0 }
    }:i32
    return chosen
}

fn main() -> i32 {
    return select(0)
}
