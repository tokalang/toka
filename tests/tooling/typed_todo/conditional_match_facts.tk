fn select(tag: i32) -> i32 {
    auto answer: i32 = todo
    auto chosen: i32 = match tag {
        0 => { pass answer }
        default => { pass 0 }
    }
    return chosen
}

fn main() -> i32 {
    return select(0)
}
