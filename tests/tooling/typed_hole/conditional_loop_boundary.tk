fn select() -> i32 {
    auto answer: i32 = hole
    auto slot#: i32 = 0
    loop false {
        slot = answer
    }
    auto observed: i32 = slot
    return observed
}

fn main() -> i32 {
    return select()
}
