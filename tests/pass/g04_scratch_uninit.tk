shape Point(x: i32, y: i32)

fn main() -> i32 {
    auto p1 = uninit:Point
    init p1 = Point(x = 10, y = 20)
    assert(p1.x == 10, "p1.x check")
    assert(p1.y == 20, "p1.y check")
    return 0
}
