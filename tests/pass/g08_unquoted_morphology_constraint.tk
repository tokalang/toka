// G: an ordinary type parameter can declare a morphology domain without quotes.
shape Bad<T>
where:
    T: morphology raw_extendable
(
    value: T
)

fn main() -> i32 {
    auto value = Bad<i32>(value = 1)
    return 0
}
