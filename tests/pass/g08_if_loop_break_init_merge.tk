fn main() -> i32 {
    auto flag = true
    auto x = uninit:i32

    if flag {
        loop {
            init x = 1
            break
        }
    } else {
        init x = 2
    }

    assert(x == 1 || x == 2, "x should be initialized by both branches")
    return 0
}
