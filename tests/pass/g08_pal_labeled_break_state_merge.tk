fn main() -> i32 {
    auto x# = uninit:i32

    auto outer = loop {
        loop {
            x = 7
            break to outer
        }
    }:()

    assert(x == 7, "labeled break should carry initialization state")
    return 0
}
