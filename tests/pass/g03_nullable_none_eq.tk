import std/io::println

fn main() -> i32 {
    auto y = none:i32?
    
    if y == none {
        println("y == none works!")
    } else {
        assert(false, "y should be none!")
    }

    auto z = 100:i32?
    if z != none {
        println("z != none works!")
    } else {
        assert(false, "z should not be none!")
    }

    return 0
}
