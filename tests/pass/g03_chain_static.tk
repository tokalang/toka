
import std/io::println

fn main() -> i32 {
    auto owner = string::from("Chaining test")
    auto v = owner.as_view()
    if v.len() != 13:usize {
        return 1
    }
    
    // Deeper chain
    auto l = string::from("Deep").as_view().len()
    if l != 4:usize {
        return 1
    }
    
    println("Chaining test passed!")
    return 0
}
