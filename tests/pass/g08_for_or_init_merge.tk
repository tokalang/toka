import std/vec::Vec

fn main() -> i32 {
    auto items# = Vec<i32>::with_capacity(0)
    auto x = uninit:i32

    for auto i in items {
        init x = i
    } or {
        init x = 0
    }

    return x
}
