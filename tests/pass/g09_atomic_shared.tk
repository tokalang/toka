import std/io::{println}
import std/sync::{Mutex}

pub fn main() -> i32 {
    auto ~m = Mutex<i32>::make_shared(cede 42)
    auto ~shared_m = ~m
    
    // Acquire
    auto ~shared_m2 = ~shared_m
    
    // Drop logic should emit Release
    return 0
}
