shape Info(value: i32)
fn main() -> i32 {
    auto info = Info(value = 7)
    auto &bad# = info.&value
    // The rejected exclusive request must not leave a loan behind.
    auto &good = info.&value
    return good
}
