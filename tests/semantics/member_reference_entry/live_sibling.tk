shape Pair(left: i32, right: i32)
fn main() -> i32 {
    auto pair = Pair(left = 1, right = 2)
    cede pair.left
    auto &view = pair.&right
    assert(view == 2, "live sibling remains borrowable")
    return 0
}
