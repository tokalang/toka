shape Info(value#: i32)
fn replace<T>(&slot#: T, cede incoming: T) { slot = cede incoming }
fn main() -> i32 {
    auto info = Info(value = 7)
    auto &prior = info.&value
    replace<i32>(info.&#value, 8)
    return prior
}
