import core/traits::{@Encap}

shape Token(value: i32)

impl Token@Encap {
    fn drop(self#) {}
}

fn make() -> Token {
    auto token = Token(value = 7:i32)
    return token
}

fn main() -> i32 {
    auto token = make()
    return token.value - 7:i32
}
