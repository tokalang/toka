import core/intrinsics/atomic::{load, Ordering}
shape Value(number:i32)
fn main() -> i32 {
    auto ^#value = new Value(number=7)
    auto result = load<^Value>(^#value, Ordering::SeqCst)
    return 0
}
