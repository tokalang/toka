fn main() -> i32 {
    auto letter = 'a'
    auto opening = '('
    auto quote = '\''
    auto raw = """ 'T '(x) """
    if letter != 'a' || opening != '(' || quote != '\'' { return 1 }
    if raw != " 'T '(x) " { return 2 }
    return 0
}
