import core/option::{Option}

shape Record(token: Option<Token>)
shape Token(value: i32)
impl Token@Encap { fn drop(self#) {} }

fn main() -> i32 {
    auto first = Token(value = 1)
    auto second = first
    return 0
}
