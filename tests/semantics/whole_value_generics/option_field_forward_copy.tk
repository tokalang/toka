import core/option::{Option}

shape Record(range: Option<Range>)
shape Range(start: usize, end: usize)

fn main() -> i32 {
    auto record = Record(range = Option<Range>::Some(Range(start = 1:usize, end = 2:usize)))
    if record.range.is_some() { return 0 }
    return 1
}
