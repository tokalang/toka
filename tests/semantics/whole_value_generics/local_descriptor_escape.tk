fn borrow<T>(value:T) -> &T <- value { return &value }
fn escaped(input:i32) -> &&i32 <- input {
    auto &reference = &input
    return borrow<&i32>(&reference)
}
fn main() -> i32 {
    auto number = 7:i32
    auto &&view = escaped(number)
    if view != 7 { return 1 }
    return 0
}
