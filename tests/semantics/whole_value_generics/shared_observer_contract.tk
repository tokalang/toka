shape Token(value:i32)
fn observe<T>(value:T, reader:fn(T) -> i32) -> i32 { return reader(value) }
fn main() -> i32 {
    auto ~value = new Token(value=42)
    auto reader = { item => item.value }:fn(~Token) -> i32
    if observe<~Token>(~value, reader) != 42 { return 1 }
    return 0
}
