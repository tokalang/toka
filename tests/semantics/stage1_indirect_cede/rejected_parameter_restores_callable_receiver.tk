fn main() -> i32 {
    auto callback = { value => cede value }:cede fn(cede i32) -> i32
    auto value = 7:i32
    auto rejected = cede callback(value)
    auto reused = cede callback(1:i32)
    return value + reused - 8:i32
}
