fn main() -> i32 {
    auto callback = { value => cede value }:fn(cede i32) -> i32
    auto value = 7:i32
    auto rejected = cede callback(value)
    auto reused = callback(1:i32)
    return value + rejected + reused - 8:i32
}
