shape Cell(value: i32)

fn main() -> i32 {
    auto callback = { cell => ^cell }:fn(cede ^Cell) -> ^Cell
    auto ^source = new Cell(value = 7:i32)
    auto ^result = callback(^source)
    return result.value
}
