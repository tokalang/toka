import std/thread::thread_spawn

fn main() -> i32 {
    auto *p = unsafe alloc i32(7)
    auto f = { [cede *p] =>
        return 0
    }
    auto handle# = thread_spawn<i32>(cede f).unwrap()
    return 0
}

// EXPECT: error[E0477]
