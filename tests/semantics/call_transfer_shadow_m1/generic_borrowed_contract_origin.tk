import core/string::{string}

fn consume_generic<T>(cede value: T) -> usize {
    auto moved = cede value
    return moved.len()
}

fn main() -> i32 {
    auto owner = string::from("view")
    auto view = owner.as_str()
    return consume_generic<str>(cede view) as i32 - 4:i32
}
