// EXPECT_TKI: pub fn consume_payload(cede p: Payload) -> i32
import core/traits::{@encap}

pub shape Payload(val: i32)

impl Payload@encap {
    fn drop(self#) {}
    pub fn clone(self) = delete
}

pub fn consume_payload(cede p: Payload) -> i32 {
    auto moved = cede p
    return moved.val
}
