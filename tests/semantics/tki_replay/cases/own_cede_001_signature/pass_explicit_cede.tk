// EXPECT_EVIDENCE: OWN-CEDE-001
import ./lib::{Payload, consume_payload}

fn main() -> i32 {
    auto p = Payload(val = 42)
    auto v = consume_payload(cede p)
    if v != 42 {
        return 1
    }
    return 0
}
