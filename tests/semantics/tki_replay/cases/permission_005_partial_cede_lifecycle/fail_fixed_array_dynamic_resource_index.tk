// EXPECT_ERROR: E04600
import ./lib::{Token}

fn main() -> i32 {
    auto drops# = 0
    auto values = [Token(id = 1, &drops = &drops)]:[Token; 1]
    auto index = 0
    auto taken = cede values[index]
    return taken.id
}
