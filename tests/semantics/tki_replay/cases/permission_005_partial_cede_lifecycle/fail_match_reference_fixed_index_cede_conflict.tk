import ./lib::{Token}

fn main() -> i32 {
    auto drops# = 0
    auto values = [Token(id = 1, &drops = &drops)]:[Token; 1]
    match values[0] {
        auto &borrow => {
            auto taken = cede values[0]
            return taken.id
        }
    }
}
// EXPECT_ERROR: E0440
