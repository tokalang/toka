// EXPECT_ERROR: E0410
import ./lib::{Token}

fn main() -> i32 {
    auto drops# = 0
    auto values: [Token; 1] = [Token(id = 1, &drops = &drops)]
    auto taken = cede values[0]
    return values[0].id + taken.id
}
