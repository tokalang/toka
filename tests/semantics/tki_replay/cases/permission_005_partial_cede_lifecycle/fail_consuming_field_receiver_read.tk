// EXPECT_ERROR: E0410
import ./lib::{ReceiverHolder, ReceiverToken}

fn main() -> i32 {
    auto holder = ReceiverHolder(item = ReceiverToken(id = 7))
    auto value = holder.item#.consume()
    return holder.item.id
}
