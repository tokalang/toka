// EXPECT_ERROR: E0410
import ./lib::{Token}

fn main() -> i32 {
    auto drops# = 0
    auto values = [Token(id = 1, &drops = &drops)]:[Token; 1]
    if true {
        auto taken = cede values[0]
    }
    return values[0].id
}
