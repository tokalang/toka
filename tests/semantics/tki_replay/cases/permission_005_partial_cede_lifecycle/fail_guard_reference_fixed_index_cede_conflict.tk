import ./lib::{Token}

fn main() -> i32 {
    auto drops# = 0
    auto values: [Token; 1] = [Token(id = 1, &drops = &drops)]
    guard auto &borrow = values[0] else { return 1 }
    auto taken = cede values[0]
    return taken.id
}
// EXPECT_ERROR: E0440
