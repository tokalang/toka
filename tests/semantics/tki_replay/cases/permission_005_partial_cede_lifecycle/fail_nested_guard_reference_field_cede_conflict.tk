import ./lib::{make_pair}

fn main() -> i32 {
    auto drops# = 0
    auto pair = make_pair(drops)
    guard auto Pair(&borrow = .&left, ..) = pair else { return 1 }
    auto taken = cede pair.left
    return taken.id
}
// EXPECT_ERROR: E0440
