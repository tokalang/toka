import ./lib::{make_pair}

fn main() -> i32 {
    auto drops# = 0
    {
        auto i# = 0
        auto pair = make_pair(drops)
        loop i < 1 {
            auto taken = cede pair.left
            if taken.id != 1 { return 1 }
            i += 1
        }
        if pair.right.id != 2 { return 2 }
    }
    if drops != 2 { return 3 }
    return 0
}
