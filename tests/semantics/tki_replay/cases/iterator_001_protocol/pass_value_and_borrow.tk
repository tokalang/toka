import ./lib::{Values}

pub fn main() -> i32 {
    auto values = Values(value = 7)
    auto total# = 0

    for auto value in values {
        total += value
    }
    for auto &value in values {
        total += value
    }

    return total - 14
}
