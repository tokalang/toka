// EXPECT_ERROR: E04581
// EXPECT_EVIDENCE: OWN-RESOURCE-001
import ./lib::{make_box, read_id}

fn main() -> i32 {
    auto box = make_box()
    auto copied = { [copy box] => read_id(box) }:fn() -> i32
    return copied()
}
