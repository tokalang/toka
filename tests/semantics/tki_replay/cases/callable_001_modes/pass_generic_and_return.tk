import ./lib::{invoke_mut}

pub fn main() -> i32 {
    auto offset = 3
    auto callback#: fn#(i32) -> i32 = { value =>
        offset + value
    }
    if invoke_mut(callback, 4) != 7 {
        return 1
    }

    return 0
}
