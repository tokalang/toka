pub shape Packet(value: i32)

pub shape BuildOutcome(
    Ok(i32) |
    Err(i32)
)

pub fn try_build(init out: Packet, fail: bool) -> BuildOutcome
outcomes:
    Ok => out: init
    Err => out: uninit
{
    if fail {
        return BuildOutcome::Err(1)
    }
    init out = Packet(value = 42)
    return BuildOutcome::Ok(0)
}
