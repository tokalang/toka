// EXPECT_ERROR: E04571
import ./lib::{Cell, ReadonlyHolder, overwrite}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto holder# = ReadonlyHolder(~cell = ~source)
    return overwrite(holder.~cell)
}
