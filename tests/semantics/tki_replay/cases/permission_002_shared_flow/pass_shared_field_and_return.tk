import ./lib::{Cell, Holder, expose}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto ~view# = expose(~source)
    auto holder = Holder(~cell = ~view)
    view.value = 2
    if source.value != 2 { return 1 }
    return 0
}
