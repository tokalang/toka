// EXPECT_ERROR: E04571
import ./lib::{Cell, cede_overwrite, ReadonlyHolder}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto holder# = ReadonlyHolder(~cell = ~source)
    return cede_overwrite(cede holder.~cell)
}
