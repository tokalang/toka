import ./lib::{Cell, RebindableHolder}

fn main() -> i32 {
    auto holder = RebindableHolder(~cell = new Cell(value = 2))
    auto ~source = new Cell(value = 1)
    holder.~cell = cede ~source
    holder.~cell = new Cell(value = 4)
    holder.cell.value = 5
    return holder.cell.value - 5
}
