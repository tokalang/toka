// EXPECT_ERROR: E04573
import ./lib::{Cell, ReadonlyHolder}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto holder = ReadonlyHolder(~cell = ~source)
    auto ~view = holder.~cell
    auto invoke# = { [cede view] =>
        view.value = 2
        return 0
    }:fn#() -> i32
    return invoke#()
}
