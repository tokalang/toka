// EXPECT_ERROR: E04573
import ./lib::{Cell}

fn main() -> i32 {
    auto ~source# = new Cell(value = 1)
    auto values# = [~source]:[~Cell; 1]
    auto ~view# = ~values[0]
    view.value = 12
    return 0
}
