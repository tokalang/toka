// EXPECT_ERROR: E0442
// EXPECT_EVIDENCE: EFF-RET-001
import ./lib::{TextData, borrow_text}

fn main() -> i32 {
    auto data# = TextData(text = "hello")
    auto text = borrow_text(data)
    data = TextData(text = "goodbye")
    return text.len() as i32
}
