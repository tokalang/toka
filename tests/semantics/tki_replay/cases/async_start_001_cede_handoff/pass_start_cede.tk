// COMPILE_ONLY: async task runtime symbols are not linked by this replay runner.
import ./lib::{Payload, consume_async}

fn main() -> i32 {
    auto payload = Payload(val = 7)
    consume_async(cede payload).start
    return 0
}
