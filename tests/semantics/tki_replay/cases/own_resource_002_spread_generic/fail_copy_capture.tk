// EXPECT_ERROR: E04581
// EXPECT_EVIDENCE: OWN-RESOURCE-001
import ./lib::{make_vault, read_id}

fn main() -> i32 {
    auto vault = make_vault()
    auto copied = { [copy vault] => read_id(vault) }:fn() -> i32
    return copied()
}
