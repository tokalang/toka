import ./lib::{Cell}

fn main() -> i32 {
    auto values: [Cell?; 1] = [Cell(value = 7)]
    auto target = cede values[0]
    guard target {
        return target.value - 7
    } else {
        return 1
    }
}
