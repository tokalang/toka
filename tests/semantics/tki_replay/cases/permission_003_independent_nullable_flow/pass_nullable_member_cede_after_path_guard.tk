import ./lib::{Cell}

shape Pair(item: Cell?)

fn main() -> i32 {
    auto pair = Pair(item = Cell(value = 7))
    if pair.item is null {
        return 1
    } else {
        auto target = cede pair.item:Cell
        return target.value - 7
    }
}
