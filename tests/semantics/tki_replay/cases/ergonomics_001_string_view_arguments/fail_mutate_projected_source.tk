// EXPECT_ERROR: E0441
import ./lib::imported_identity

fn main() -> i32 {
    auto owned# = string::from("borrowed")
    auto view = imported_identity(owned)
    owned#.push_str("!")
    return view.len() as i32
}
