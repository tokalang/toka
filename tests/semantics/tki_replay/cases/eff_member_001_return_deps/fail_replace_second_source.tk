import ./lib::{Engine, split_engines}

fn main() -> i32 {
    auto e1# = Engine(val = 3)
    auto e2# = Engine(val = 4)
    auto refs = split_engines(e1, e2)
    e2 = Engine(val = 5) // EXPECT_ERROR: E0442
    return refs.second
}
