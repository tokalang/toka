import ./lib::{Engine, split_engines}

fn main() -> i32 {
    auto e1# = Engine(val = 3)
    auto e2# = Engine(val = 4)
    auto first = split_engines(e1, e2).first
    e1 = Engine(val = 5)
    e2 = Engine(val = 6)
    return first - 3
}
