import ./lib::{get_external_view}
import std/task
fn main() -> i32 {
    auto owner = string::from("hello")
    auto view = get_external_view(owner.as_view()).wait
    assert(view.equals("hello"), "task cleanup preserves the external live owner")
    return 0
}
