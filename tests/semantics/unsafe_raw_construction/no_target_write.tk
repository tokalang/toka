fn main() -> i32 {
    auto *storage# = unsafe alloc [1] i32
    auto address# = unsafe *storage as Addr
    auto *writer# = unsafe (address as *i32)
    unsafe {
        writer = 17
        if storage[0] != 17 { return 1 }
        free [1] *storage
    }
    return 0
}
