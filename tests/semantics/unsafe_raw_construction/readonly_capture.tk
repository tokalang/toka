fn main() -> i32 {
    auto value = 7:i32
    auto address = unsafe (&value as Addr)
    auto callback = { [copy address] =>
        auto *writer# = unsafe (address as *i32)
        return 0:i32
    }:fn() -> i32
    return callback()
}
