import std/vec::{Vec}
fn main() -> i32 {
    auto values# = Vec<string>::new()
    values#.push(string::from("owned"))
    auto result = values#.pop()
    auto value = result.unwrap()
    if !value.as_str().equals("owned") || values.len() != 0 { return 1 }
    return 0
}
