// The changed consuming signature rejects bare spelling before any move.
// EXPECT_ERROR: E04570
import ./lib::{Payload, consume}

fn main() -> i32 {
    auto payload = Payload(val = 1)
    consume(payload)
    return payload.val
}
