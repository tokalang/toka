// The removed explicit method must invalidate cached interfaces.
// EXPECT_ERROR: E0417
import ./lib::{make_resource, read_resource}

fn main() -> i32 {
    auto resource = make_resource()
    auto copied = resource.clone()
    return read_resource(copied)
}
