import ./lib as remote

shape Box<T>(
    remote: bool,
    marker: u8,
    item: T
)

fn main() -> i32 {
    auto local = Box<i32>(remote = true, marker = 2, item = 7)
    if !local.remote || local.marker != 2 {
        return 1
    }
    return 0
}
