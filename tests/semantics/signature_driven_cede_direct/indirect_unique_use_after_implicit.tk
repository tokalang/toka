shape Cell(value: i32)

fn main() -> i32 {
    auto callback = { cell =>
        return cede ^cell
    }:fn(cede ^Cell) -> cede ^Cell
    auto ^source = new Cell(value = 31:i32)
    auto ^value = cede callback(^source)
    return value.value + source.value
}
