shape Cell(value: i32)

fn consume(cede ^cell: Cell) -> i32 {
    auto ^owned = cede ^cell
    return owned.value
}

fn main() -> i32 {
    auto ^source = new Cell(value = 1:i32)
    auto value = consume(^source)
    return value + source.value
}
