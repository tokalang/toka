import std/slab::{SlabID}
fn consume_id(cede value: SlabID) -> usize {
    auto moved = cede value
    return moved.index()
}

fn main() -> i32 {
    auto value = SlabID::new(1:usize, 1:usize)
    return consume_id(value) as i32 - 1:i32
}
