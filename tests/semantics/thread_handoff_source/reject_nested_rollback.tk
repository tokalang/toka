import core/intrinsics/thread_handoff_probe::{__toka_thread_probe_run}
shape Token(value: i32)
impl Token@Encap {
    pub value
    fn drop(self#) {}
}
impl Token@Send {}
fn main() -> i32 {
    auto token = Token(value = 7)
    auto rejected = __toka_thread_probe_run<bool, dyn fn() -> i32>(
        { [cede token] => token.value }:dyn fn() -> i32)
    return token.value
}
