shape Token(value: i32)
fn borrowed(~value: Token) -> async i32 { return value.value }
fn main() -> i32 {
    auto ~owner = new Token(value = 42)
    auto task = borrowed(~owner).start
    return 0
}
