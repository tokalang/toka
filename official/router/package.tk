// Static manifest for the official/router package.
pub const PACKAGE = (
    name = "router",
    identity = "official/router",
    version = "0.1.0",
    kind = "library",
    license = "Apache-2.0",
    compiler = "0.9.9-01",
    entry_modules = ("lib/official/router.tk"),
    targets = ("macos", "linux"),
    dependencies = (),
    native = (required = false, libraries = ())
)

pub const AI_CONTRACT = (
    schema = "toka-official-package-v1",
    summary = "Deterministic HTTP method/path recognition with named path parameters.",
    capabilities = ("register static and :parameter path segments", "recognize routes with static-segment precedence", "report allowed methods for application-owned 405 responses"),
    non_goals = ("HTTP connection ownership", "handler invocation", "middleware", "wildcards", "path normalization", "percent decoding"),
    safety = ("route definitions are validated before registration; equal-specificity overlap is rejected; query text never changes route selection"),
    qualification = ("../../build/bin/tokac -I ../../lib -I lib tests/router_v1.tk -o /tmp/router_v1 && /tmp/router_v1; python3 tests/qualify_package.py")
)
