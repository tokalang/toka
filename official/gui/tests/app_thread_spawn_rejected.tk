import official/gui::{App}
import std/thread::{thread_spawn}

fn launch(cede app: App) {
    auto worker = { [cede app] =>
        return cede 0
    }:fn() -> cede i32
    auto handle# = thread_spawn<i32>(cede worker)
    handle#.detach()
}

fn main() -> i32 { return 0 }

// EXPECT: error[E0477]
