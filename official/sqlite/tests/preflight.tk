import official/sqlite::{package_name}

extern fn toka_sqlite_preflight() -> i32

fn main() -> i32 {
    assert(package_name().equals("sqlite"), "official SQLite package entry must resolve")
    assert(toka_sqlite_preflight() == 0:i32,
           "selected official/sqlite package must link and execute against SQLite")
    return 0
}
