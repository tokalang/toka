// Static manifest for the official/sqlite package.
pub const PACKAGE = (
    name = "sqlite",
    identity = "official/sqlite",
    version = "0.1.0",
    kind = "library",
    license = "Apache-2.0",
    compiler = "1.0.0-rc.3",
    entry_modules = ("lib/official/sqlite.tk"),
    targets = ("macos", "linux"),
    dependencies = (),
    native = (
        required = true,
        sources = ("native/sqlite_preflight.c"),
        macos = (pkg_config = ("sqlite3")),
        linux = (pkg_config = ("sqlite3"))
    )
)

pub const AI_CONTRACT = (
    schema = "toka-official-package-v1",
    summary = "Safe, synchronous bridge to the system SQLite C library.",
    capabilities = ("native SQLite ABI preflight", "safe open execute close lifecycle", "prepared scalar statements", "explicit transactions"),
    non_goals = ("ORM", "SQL parser", "connection pool", "async database API", "nested transactions"),
    safety = ("SQLite is an opt-in package dependency; no public API exposes native handles"),
    qualification = ("python3 tests/qualify_preflight.py")
)
